package Lesson;

// interfaces are abstract classes that serve as proper blueprints
// note that they do not need the abstract keyword in order to work
// methods and members will always be implicitly abstract
public interface Catchable {
    // note the lack of a method body!
    public void hasBeenCaught();
}
